# NN-风格名

> 复刻此配方时：只换内容，不换设计语言。所有 hex/字体/签名元素照抄。

## 元信息

- **风格名**：
- **来源**：（仓库 URL 或参考样例）
- **一句话气质**：
- **适合场景**：（什么样的旅行/内容适合用）
- **已用于**：（哪次旅行，示例 HTML 路径）

## 配色（`:root`）

```css
:root{
  /* 底色 / 墨色 */
  /* 强调色（注明哪个是单一锚点） */
  /* 辅助色 / 线色 */
}
```

配色纪律：（如：单锚点色、浅底高亮必须纯黑、spec_lock 六字段…）

## 字体

- **标题体**：`字体栈（含中文 fallback）`
- **正文体**：`字体栈`
- **铬件/等宽**：`字体栈`（kicker/标签/数据用）
- **字号策略**：（如 hero 11vw×200 字重 vs 14px 铬件）
- **加载**：系统字体栈 / 需 Google Font @import / 断网 fallback 说明

## 全局纪律

- 圆角：允许 / 禁止（如 `*{border-radius:0!important}`）
- 阴影：允许 / 禁止
- 玻璃/模糊：允许 / 禁止
- 动效：只许 transform/opacity / 其他限制
- 其他：

## 签名元素（3-5 个，附 CSS）

> 「看到它就知道是这个风格」的元素。每个给可直接复制的 CSS。

### 1. 元素名

说明它为什么是签名。
```css
/* CSS 片段 */
```

### 2. 元素名
...

### 3. 元素名
...

## 页面骨架（旅行攻略标准章节）

```
1. 导航 nav（sticky）
2. Hero（标题 + 路线 + 关键 chips + 倒计时/提醒）
3. 关键预约 / 待办 prep（行前盯紧的事）
4. 逐日行程 days（时间线）
5. 住宿 shuizhu
6. 行前清单 checklist
7. 花销 expenses
8. 路线图 route（SVG，风格化）
9. 笔记/日记 notes
10. 照片 photos（IndexedDB 上传）
11. 页脚 footer
```

每节在本风格里用什么版式：（如：days 用时间线左竖线、expenses 用表格、photos 用 masonry…）

## 组件配方

### 导航 nav
```css
/* CSS */
```

### Hero
```css
/* CSS */
```

### 章节标题 sec-title
```css
/* CSS */
```

### chip（关键信息标签）
```css
/* CSS */
```

### 待办 prep / 清单 checklist
```css
/* CSS */
```

### 时间线 timeline（逐日）
```css
/* CSS */
```

### 卡片 card（住宿/花销）
```css
/* CSS */
```

### 照片 photos
```css
/* CSS */
```

### 页脚 footer
```css
/* CSS */
```

## 反模式（不要这样做）

- ❌ ……（会破坏风格）
- ❌ ……

## 示例

- 配方出处：（本风格的示例 HTML 路径，如 `2026-十一平潭自驾/风格试验/01-瑞士国际主义.html`）
- 在线：（GitHub Pages 链接，若有）
